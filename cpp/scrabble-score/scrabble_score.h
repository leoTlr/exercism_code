#if !defined(SCRABBLE_SCORE_H)
#define SCRABBLE_SCORE_H

#include <string>
#include <cinttypes>

namespace scrabble_score {

    size_t score(std::string const& word);

}  // namespace scrabble_score

#endif // SCRABBLE_SCORE_H